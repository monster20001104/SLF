// This file was autogenerated by PeakRDL-verilog
module virtio_rx_buf_dfx #(
    parameter                                ADDR_OFFSET = 0,  //! Module's offset in the main address map
    parameter                                ADDR_WIDTH  = 32,   //! Width of SW address bus
    parameter                                DATA_WIDTH  = 32    //! Width of SW data bus
)(
    // Clocks and resets
    input logic                              clk,     //! Default clock
    input logic                              rst,  //! Default reset

    // Register RX_BUF_CHKSUM_ENABLE
    output logic                     [ 0: 0] rx_buf_chksum_enable_rx_buf_chksum_enable_q,              //! Current field value

    // Register RX_BUF_SCH_WEIGHT
    output logic                     [ 7: 0] rx_buf_sch_weight_rx_buf_sch_weight_q,              //! Current field value

    // Register RX_BUF_TIME_DROP_MODE
    output logic                     [ 7: 0] rx_buf_time_drop_mode_rx_buf_time_drop_mode_q,              //! Current field value

    // Register RX_BUF_RANDOM_DROP_MODE
    output logic                     [ 7: 0] rx_buf_random_drop_mode_rx_buf_random_drop_mode_q,              //! Current field value

    // Register CSUM_DROP_PKT_TOTAL
    input  logic                     [63: 0] csum_drop_pkt_total_csum_drop_pkt_total_wdata,          //! HW write data

    // Register QOS_DROP_PKT_TOTAL
    input  logic                     [63: 0] qos_drop_pkt_total_qos_drop_pkt_total_wdata,          //! HW write data

    // Register BUF_FULL_DROP_PKT_TOTAL
    input  logic                     [63: 0] buf_full_drop_pkt_total_buf_full_drop_pkt_total_wdata,          //! HW write data

    // Register NOT_READY_DROP_PKT_TOTAL
    input  logic                     [63: 0] not_ready_drop_pkt_total_not_ready_drop_pkt_total_wdata,          //! HW write data

    // Register NETRX_IF_PKT_CNT
    input  logic                     [63: 0] netrx_if_pkt_cnt_netrx_if_pkt_cnt_wdata,          //! HW write data

    // Register VIRTIO_RX_BUF_DFX_ERR
    input  logic                             virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_we,             //! Control HW write (active high)
    input  logic                     [63: 0] virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_wdata,          //! HW write data
    output logic                     [63: 0] virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q,              //! Current field value

    // Register VIRTIO_RX_BUF_DFX_LINK_ERR
    input  logic                             virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_we,             //! Control HW write (active high)
    input  logic                     [63: 0] virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_wdata,          //! HW write data
    output logic                     [63: 0] virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q,              //! Current field value

    // Register VIRTIO_RX_BUF_DFX_LINK_STAT
    input  logic                     [63: 0] virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_wdata,          //! HW write data

    // Register VIRTIO_RX_BUF_DFX_DROP_STAT
    input  logic                     [63: 0] virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_wdata,          //! HW write data

    // Register Bus
    mlite_if.slave                           csr_if

);

/* verilator lint_off UNUSED */
    // local output signals for fields (unless block outputs)
    // these can be used as references in other fields
    logic                           [ 0: 0] rx_buf_chksum_enable_rx_buf_chksum_enable_next;
    logic                             rx_buf_chksum_enable_rx_buf_chksum_enable_anded;
    logic                             rx_buf_chksum_enable_rx_buf_chksum_enable_ored;
    logic                             rx_buf_chksum_enable_rx_buf_chksum_enable_xored;
    logic                           [ 7: 0] rx_buf_sch_weight_rx_buf_sch_weight_next;
    logic                             rx_buf_sch_weight_rx_buf_sch_weight_anded;
    logic                             rx_buf_sch_weight_rx_buf_sch_weight_ored;
    logic                             rx_buf_sch_weight_rx_buf_sch_weight_xored;
    logic                           [ 7: 0] rx_buf_time_drop_mode_rx_buf_time_drop_mode_next;
    logic                             rx_buf_time_drop_mode_rx_buf_time_drop_mode_anded;
    logic                             rx_buf_time_drop_mode_rx_buf_time_drop_mode_ored;
    logic                             rx_buf_time_drop_mode_rx_buf_time_drop_mode_xored;
    logic                           [ 7: 0] rx_buf_random_drop_mode_rx_buf_random_drop_mode_next;
    logic                             rx_buf_random_drop_mode_rx_buf_random_drop_mode_anded;
    logic                             rx_buf_random_drop_mode_rx_buf_random_drop_mode_ored;
    logic                             rx_buf_random_drop_mode_rx_buf_random_drop_mode_xored;
    logic                           [63: 0] csum_drop_pkt_total_csum_drop_pkt_total_q;
    logic                           [63: 0] csum_drop_pkt_total_csum_drop_pkt_total_next;
    logic                             csum_drop_pkt_total_csum_drop_pkt_total_anded;
    logic                             csum_drop_pkt_total_csum_drop_pkt_total_ored;
    logic                             csum_drop_pkt_total_csum_drop_pkt_total_xored;
    logic                           [63: 0] qos_drop_pkt_total_qos_drop_pkt_total_q;
    logic                           [63: 0] qos_drop_pkt_total_qos_drop_pkt_total_next;
    logic                             qos_drop_pkt_total_qos_drop_pkt_total_anded;
    logic                             qos_drop_pkt_total_qos_drop_pkt_total_ored;
    logic                             qos_drop_pkt_total_qos_drop_pkt_total_xored;
    logic                           [63: 0] buf_full_drop_pkt_total_buf_full_drop_pkt_total_q;
    logic                           [63: 0] buf_full_drop_pkt_total_buf_full_drop_pkt_total_next;
    logic                             buf_full_drop_pkt_total_buf_full_drop_pkt_total_anded;
    logic                             buf_full_drop_pkt_total_buf_full_drop_pkt_total_ored;
    logic                             buf_full_drop_pkt_total_buf_full_drop_pkt_total_xored;
    logic                           [63: 0] not_ready_drop_pkt_total_not_ready_drop_pkt_total_q;
    logic                           [63: 0] not_ready_drop_pkt_total_not_ready_drop_pkt_total_next;
    logic                             not_ready_drop_pkt_total_not_ready_drop_pkt_total_anded;
    logic                             not_ready_drop_pkt_total_not_ready_drop_pkt_total_ored;
    logic                             not_ready_drop_pkt_total_not_ready_drop_pkt_total_xored;
    logic                           [63: 0] netrx_if_pkt_cnt_netrx_if_pkt_cnt_q;
    logic                           [63: 0] netrx_if_pkt_cnt_netrx_if_pkt_cnt_next;
    logic                             netrx_if_pkt_cnt_netrx_if_pkt_cnt_anded;
    logic                             netrx_if_pkt_cnt_netrx_if_pkt_cnt_ored;
    logic                             netrx_if_pkt_cnt_netrx_if_pkt_cnt_xored;
    logic                           [63: 0] virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_next;
    logic                             virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_anded;
    logic                             virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_ored;
    logic                             virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_xored;
    logic                           [63: 0] virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_next;
    logic                             virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_anded;
    logic                             virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_ored;
    logic                             virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_xored;
    logic                           [63: 0] virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_q;
    logic                           [63: 0] virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_next;
    logic                             virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_anded;
    logic                             virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_ored;
    logic                             virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_xored;
    logic                           [63: 0] virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_q;
    logic                           [63: 0] virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_next;
    logic                             virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_anded;
    logic                             virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_ored;
    logic                             virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_xored;
/* verilator lint_on UNUSED */

    // ============================================================
    // SW Access logic
    // ============================================================
    logic sw_wr;
    logic sw_rd;
    /* verilator lint_off UNUSED */
    logic [DATA_WIDTH-1:0] sw_mask;
    logic [DATA_WIDTH-1:0] sw_wdata;
    logic [DATA_WIDTH-1:0] sw_rdata;
    logic [DATA_WIDTH-1:0] sw_masked_data;
    /* verilator lint_on UNUSED */

    // convert bus interface to internal sw_* signals
    logic [ADDR_WIDTH-1:0]      csr_if_addr;
    logic [DATA_WIDTH-1:0]      csr_if_wdata;
    logic [DATA_WIDTH-1:0]      csr_if_rdata;
    logic [DATA_WIDTH/8-1:0]    csr_if_wmask;


    logic [ADDR_WIDTH-1:0]      addr;
    logic [DATA_WIDTH-1:0]      rdata;
    logic [DATA_WIDTH/8-1:0]    wmask;

    // convert byte mask to bit mask
    always @(*) begin
        int byte_idx;
        for (byte_idx = 0; byte_idx < DATA_WIDTH/8; byte_idx+=1) begin
            sw_mask[8*(byte_idx+1)-1 -: 8] = {8{wmask[byte_idx]}};
        end
    end

    enum logic [3:0]  { 
        IDLE     = 4'b0001,
        WRITE    = 4'b0010,
        READ     = 4'b0100,
        READ_RSP = 4'b1000
    } cstat, nstat;

    always @(posedge clk) begin
        if(rst)begin
            cstat <= IDLE;
        end else begin
            cstat <= nstat;
        end
    end

    always @(*)begin
        nstat = cstat;
        case (cstat)
            IDLE: begin
                if(csr_if.ready && csr_if.valid)begin
                    if(!csr_if.read)begin
                        nstat = WRITE;
                    end else begin
                        nstat = READ;
                    end
                end
            end
            WRITE:begin
                nstat = IDLE;
            end
            READ:begin
                nstat = READ_RSP;
            end
            READ_RSP:begin
                if(csr_if.rready)begin
                    nstat = IDLE;
                end
            end
            default:begin
                nstat = cstat;
            end
        endcase
    end

    always @(posedge clk) begin
        if(cstat == IDLE)begin
            csr_if_addr   <= csr_if.addr;
            csr_if_wdata  <= csr_if.wdata;
            csr_if_wmask  <= csr_if.wmask;
        end
    end


    assign addr     = csr_if_addr;
    assign sw_wr    = cstat == WRITE;
    assign sw_rd    = cstat == READ;
    assign sw_wdata = csr_if_wdata;
    assign wmask    = csr_if_wmask;
    assign rdata    = sw_rdata;

    always @(posedge clk) begin
        if(cstat == READ)begin
            csr_if_rdata <= rdata;
        end
    end

    assign csr_if.ready   = cstat == IDLE;
    assign csr_if.rvalid  = cstat == READ_RSP;
    assign csr_if.rdata   = csr_if_rdata;

    // helpful masked version of data
    assign sw_masked_data = sw_wdata & sw_mask;
    logic                     [DATA_WIDTH-1:0] rx_buf_chksum_enable_rdata;
    logic                     [DATA_WIDTH-1:0] rx_buf_sch_weight_rdata;
    logic                     [DATA_WIDTH-1:0] rx_buf_time_drop_mode_rdata;
    logic                     [DATA_WIDTH-1:0] rx_buf_random_drop_mode_rdata;
    logic                     [DATA_WIDTH-1:0] csum_drop_pkt_total_rdata;
    logic                     [DATA_WIDTH-1:0] qos_drop_pkt_total_rdata;
    logic                     [DATA_WIDTH-1:0] buf_full_drop_pkt_total_rdata;
    logic                     [DATA_WIDTH-1:0] not_ready_drop_pkt_total_rdata;
    logic                     [DATA_WIDTH-1:0] netrx_if_pkt_cnt_rdata;
    logic                     [DATA_WIDTH-1:0] virtio_rx_buf_dfx_err_rdata;
    logic                     [DATA_WIDTH-1:0] virtio_rx_buf_dfx_link_err_rdata;
    logic                     [DATA_WIDTH-1:0] virtio_rx_buf_dfx_link_stat_rdata;
    logic                     [DATA_WIDTH-1:0] virtio_rx_buf_dfx_drop_stat_rdata;

    assign sw_rdata = // or of each register return (masked)
                   rx_buf_chksum_enable_rdata | 
                   rx_buf_sch_weight_rdata | 
                   rx_buf_time_drop_mode_rdata | 
                   rx_buf_random_drop_mode_rdata | 
                   csum_drop_pkt_total_rdata | 
                   qos_drop_pkt_total_rdata | 
                   buf_full_drop_pkt_total_rdata | 
                   not_ready_drop_pkt_total_rdata | 
                   netrx_if_pkt_cnt_rdata | 
                   virtio_rx_buf_dfx_err_rdata | 
                   virtio_rx_buf_dfx_link_err_rdata | 
                   virtio_rx_buf_dfx_link_stat_rdata | 
                   virtio_rx_buf_dfx_drop_stat_rdata;
            
        
    // ============================================================
    // Register: RX_BUF_CHKSUM_ENABLE
    //    [ 0: 0] rx_buf_chksum_enable: hw=r     sw=rw    reset=0x1
    // ============================================================
    logic                  rx_buf_chksum_enable_decode;
    logic                  rx_buf_chksum_enable_sw_wr;
    logic                  rx_buf_chksum_enable_sw_rd;
    logic [DATA_WIDTH-1:0] rx_buf_chksum_enable_q;

    assign rx_buf_chksum_enable_decode = (addr == (ADDR_OFFSET+'h0+'h0));
    assign rx_buf_chksum_enable_sw_wr = sw_wr && rx_buf_chksum_enable_decode;
    assign rx_buf_chksum_enable_sw_rd = sw_rd && rx_buf_chksum_enable_decode;

    always_comb begin
        rx_buf_chksum_enable_q = '0;
        rx_buf_chksum_enable_q[ 0: 0] = rx_buf_chksum_enable_rx_buf_chksum_enable_q;
    end

    // masked version of return data
    assign rx_buf_chksum_enable_rdata = rx_buf_chksum_enable_sw_rd ? rx_buf_chksum_enable_q : 'b0;

    // ------------------------------------------------------------
    // Field: rx_buf_chksum_enable
    // ------------------------------------------------------------
    assign rx_buf_chksum_enable_rx_buf_chksum_enable_anded = & rx_buf_chksum_enable_rx_buf_chksum_enable_q;
    assign rx_buf_chksum_enable_rx_buf_chksum_enable_ored  = | rx_buf_chksum_enable_rx_buf_chksum_enable_q;
    assign rx_buf_chksum_enable_rx_buf_chksum_enable_xored = ^ rx_buf_chksum_enable_rx_buf_chksum_enable_q;

    // next hardware value

    //! main storage
    always_ff @ (posedge clk)
    if (rst) begin
        rx_buf_chksum_enable_rx_buf_chksum_enable_q <= 64'd1;
    end else begin
        // Software write
        if (rx_buf_chksum_enable_sw_wr) begin
            rx_buf_chksum_enable_rx_buf_chksum_enable_q <=  sw_masked_data[ 0: 0] | (rx_buf_chksum_enable_rx_buf_chksum_enable_q & ~sw_mask[ 0: 0]);
        end
    end
            
        
    // ============================================================
    // Register: RX_BUF_SCH_WEIGHT
    //    [ 7: 0]    rx_buf_sch_weight: hw=r     sw=rw    reset=0x21
    // ============================================================
    logic                  rx_buf_sch_weight_decode;
    logic                  rx_buf_sch_weight_sw_wr;
    logic                  rx_buf_sch_weight_sw_rd;
    logic [DATA_WIDTH-1:0] rx_buf_sch_weight_q;

    assign rx_buf_sch_weight_decode = (addr == (ADDR_OFFSET+'h0+'h8));
    assign rx_buf_sch_weight_sw_wr = sw_wr && rx_buf_sch_weight_decode;
    assign rx_buf_sch_weight_sw_rd = sw_rd && rx_buf_sch_weight_decode;

    always_comb begin
        rx_buf_sch_weight_q = '0;
        rx_buf_sch_weight_q[ 7: 0] = rx_buf_sch_weight_rx_buf_sch_weight_q;
    end

    // masked version of return data
    assign rx_buf_sch_weight_rdata = rx_buf_sch_weight_sw_rd ? rx_buf_sch_weight_q : 'b0;

    // ------------------------------------------------------------
    // Field: rx_buf_sch_weight
    // ------------------------------------------------------------
    assign rx_buf_sch_weight_rx_buf_sch_weight_anded = & rx_buf_sch_weight_rx_buf_sch_weight_q;
    assign rx_buf_sch_weight_rx_buf_sch_weight_ored  = | rx_buf_sch_weight_rx_buf_sch_weight_q;
    assign rx_buf_sch_weight_rx_buf_sch_weight_xored = ^ rx_buf_sch_weight_rx_buf_sch_weight_q;

    // next hardware value

    //! main storage
    always_ff @ (posedge clk)
    if (rst) begin
        rx_buf_sch_weight_rx_buf_sch_weight_q <= 64'd33;
    end else begin
        // Software write
        if (rx_buf_sch_weight_sw_wr) begin
            rx_buf_sch_weight_rx_buf_sch_weight_q <=  sw_masked_data[ 7: 0] | (rx_buf_sch_weight_rx_buf_sch_weight_q & ~sw_mask[ 7: 0]);
        end
    end
            
        
    // ============================================================
    // Register: RX_BUF_TIME_DROP_MODE
    //    [ 7: 0] rx_buf_time_drop_mode: hw=r     sw=rw    reset=0x0
    // ============================================================
    logic                  rx_buf_time_drop_mode_decode;
    logic                  rx_buf_time_drop_mode_sw_wr;
    logic                  rx_buf_time_drop_mode_sw_rd;
    logic [DATA_WIDTH-1:0] rx_buf_time_drop_mode_q;

    assign rx_buf_time_drop_mode_decode = (addr == (ADDR_OFFSET+'h0+'h10));
    assign rx_buf_time_drop_mode_sw_wr = sw_wr && rx_buf_time_drop_mode_decode;
    assign rx_buf_time_drop_mode_sw_rd = sw_rd && rx_buf_time_drop_mode_decode;

    always_comb begin
        rx_buf_time_drop_mode_q = '0;
        rx_buf_time_drop_mode_q[ 7: 0] = rx_buf_time_drop_mode_rx_buf_time_drop_mode_q;
    end

    // masked version of return data
    assign rx_buf_time_drop_mode_rdata = rx_buf_time_drop_mode_sw_rd ? rx_buf_time_drop_mode_q : 'b0;

    // ------------------------------------------------------------
    // Field: rx_buf_time_drop_mode
    // ------------------------------------------------------------
    assign rx_buf_time_drop_mode_rx_buf_time_drop_mode_anded = & rx_buf_time_drop_mode_rx_buf_time_drop_mode_q;
    assign rx_buf_time_drop_mode_rx_buf_time_drop_mode_ored  = | rx_buf_time_drop_mode_rx_buf_time_drop_mode_q;
    assign rx_buf_time_drop_mode_rx_buf_time_drop_mode_xored = ^ rx_buf_time_drop_mode_rx_buf_time_drop_mode_q;

    // next hardware value

    //! main storage
    always_ff @ (posedge clk)
    if (rst) begin
        rx_buf_time_drop_mode_rx_buf_time_drop_mode_q <= 64'd0;
    end else begin
        // Software write
        if (rx_buf_time_drop_mode_sw_wr) begin
            rx_buf_time_drop_mode_rx_buf_time_drop_mode_q <=  sw_masked_data[ 7: 0] | (rx_buf_time_drop_mode_rx_buf_time_drop_mode_q & ~sw_mask[ 7: 0]);
        end
    end
            
        
    // ============================================================
    // Register: RX_BUF_RANDOM_DROP_MODE
    //    [ 7: 0] rx_buf_random_drop_mode: hw=r     sw=rw    reset=0x0
    // ============================================================
    logic                  rx_buf_random_drop_mode_decode;
    logic                  rx_buf_random_drop_mode_sw_wr;
    logic                  rx_buf_random_drop_mode_sw_rd;
    logic [DATA_WIDTH-1:0] rx_buf_random_drop_mode_q;

    assign rx_buf_random_drop_mode_decode = (addr == (ADDR_OFFSET+'h0+'h18));
    assign rx_buf_random_drop_mode_sw_wr = sw_wr && rx_buf_random_drop_mode_decode;
    assign rx_buf_random_drop_mode_sw_rd = sw_rd && rx_buf_random_drop_mode_decode;

    always_comb begin
        rx_buf_random_drop_mode_q = '0;
        rx_buf_random_drop_mode_q[ 7: 0] = rx_buf_random_drop_mode_rx_buf_random_drop_mode_q;
    end

    // masked version of return data
    assign rx_buf_random_drop_mode_rdata = rx_buf_random_drop_mode_sw_rd ? rx_buf_random_drop_mode_q : 'b0;

    // ------------------------------------------------------------
    // Field: rx_buf_random_drop_mode
    // ------------------------------------------------------------
    assign rx_buf_random_drop_mode_rx_buf_random_drop_mode_anded = & rx_buf_random_drop_mode_rx_buf_random_drop_mode_q;
    assign rx_buf_random_drop_mode_rx_buf_random_drop_mode_ored  = | rx_buf_random_drop_mode_rx_buf_random_drop_mode_q;
    assign rx_buf_random_drop_mode_rx_buf_random_drop_mode_xored = ^ rx_buf_random_drop_mode_rx_buf_random_drop_mode_q;

    // next hardware value

    //! main storage
    always_ff @ (posedge clk)
    if (rst) begin
        rx_buf_random_drop_mode_rx_buf_random_drop_mode_q <= 64'd0;
    end else begin
        // Software write
        if (rx_buf_random_drop_mode_sw_wr) begin
            rx_buf_random_drop_mode_rx_buf_random_drop_mode_q <=  sw_masked_data[ 7: 0] | (rx_buf_random_drop_mode_rx_buf_random_drop_mode_q & ~sw_mask[ 7: 0]);
        end
    end
            
        
    // ============================================================
    // Register: CSUM_DROP_PKT_TOTAL
    //    [63: 0]  csum_drop_pkt_total: hw=w     sw=r     reset=0x0
    // ============================================================
    logic                  csum_drop_pkt_total_decode;
    logic                  csum_drop_pkt_total_sw_wr;
    logic                  csum_drop_pkt_total_sw_rd;
    logic [DATA_WIDTH-1:0] csum_drop_pkt_total_q;

    assign csum_drop_pkt_total_decode = (addr == (ADDR_OFFSET+'h0+'h20));
    assign csum_drop_pkt_total_sw_wr = sw_wr && csum_drop_pkt_total_decode;
    assign csum_drop_pkt_total_sw_rd = sw_rd && csum_drop_pkt_total_decode;

    always_comb begin
        csum_drop_pkt_total_q = '0;
        csum_drop_pkt_total_q[63: 0] = csum_drop_pkt_total_csum_drop_pkt_total_q;
    end

    // masked version of return data
    assign csum_drop_pkt_total_rdata = csum_drop_pkt_total_sw_rd ? csum_drop_pkt_total_q : 'b0;

    // ------------------------------------------------------------
    // Field: csum_drop_pkt_total (wire)
    // ------------------------------------------------------------
    assign csum_drop_pkt_total_csum_drop_pkt_total_anded = & csum_drop_pkt_total_csum_drop_pkt_total_q;
    assign csum_drop_pkt_total_csum_drop_pkt_total_ored  = | csum_drop_pkt_total_csum_drop_pkt_total_q;
    assign csum_drop_pkt_total_csum_drop_pkt_total_xored = ^ csum_drop_pkt_total_csum_drop_pkt_total_q;

    // next hardware value
    assign csum_drop_pkt_total_csum_drop_pkt_total_next = csum_drop_pkt_total_csum_drop_pkt_total_wdata;
    assign csum_drop_pkt_total_csum_drop_pkt_total_q = csum_drop_pkt_total_csum_drop_pkt_total_next;
            
        
    // ============================================================
    // Register: QOS_DROP_PKT_TOTAL
    //    [63: 0]   qos_drop_pkt_total: hw=w     sw=r     reset=0x0
    // ============================================================
    logic                  qos_drop_pkt_total_decode;
    logic                  qos_drop_pkt_total_sw_wr;
    logic                  qos_drop_pkt_total_sw_rd;
    logic [DATA_WIDTH-1:0] qos_drop_pkt_total_q;

    assign qos_drop_pkt_total_decode = (addr == (ADDR_OFFSET+'h0+'h28));
    assign qos_drop_pkt_total_sw_wr = sw_wr && qos_drop_pkt_total_decode;
    assign qos_drop_pkt_total_sw_rd = sw_rd && qos_drop_pkt_total_decode;

    always_comb begin
        qos_drop_pkt_total_q = '0;
        qos_drop_pkt_total_q[63: 0] = qos_drop_pkt_total_qos_drop_pkt_total_q;
    end

    // masked version of return data
    assign qos_drop_pkt_total_rdata = qos_drop_pkt_total_sw_rd ? qos_drop_pkt_total_q : 'b0;

    // ------------------------------------------------------------
    // Field: qos_drop_pkt_total (wire)
    // ------------------------------------------------------------
    assign qos_drop_pkt_total_qos_drop_pkt_total_anded = & qos_drop_pkt_total_qos_drop_pkt_total_q;
    assign qos_drop_pkt_total_qos_drop_pkt_total_ored  = | qos_drop_pkt_total_qos_drop_pkt_total_q;
    assign qos_drop_pkt_total_qos_drop_pkt_total_xored = ^ qos_drop_pkt_total_qos_drop_pkt_total_q;

    // next hardware value
    assign qos_drop_pkt_total_qos_drop_pkt_total_next = qos_drop_pkt_total_qos_drop_pkt_total_wdata;
    assign qos_drop_pkt_total_qos_drop_pkt_total_q = qos_drop_pkt_total_qos_drop_pkt_total_next;
            
        
    // ============================================================
    // Register: BUF_FULL_DROP_PKT_TOTAL
    //    [63: 0] buf_full_drop_pkt_total: hw=w     sw=r     reset=0x0
    // ============================================================
    logic                  buf_full_drop_pkt_total_decode;
    logic                  buf_full_drop_pkt_total_sw_wr;
    logic                  buf_full_drop_pkt_total_sw_rd;
    logic [DATA_WIDTH-1:0] buf_full_drop_pkt_total_q;

    assign buf_full_drop_pkt_total_decode = (addr == (ADDR_OFFSET+'h0+'h30));
    assign buf_full_drop_pkt_total_sw_wr = sw_wr && buf_full_drop_pkt_total_decode;
    assign buf_full_drop_pkt_total_sw_rd = sw_rd && buf_full_drop_pkt_total_decode;

    always_comb begin
        buf_full_drop_pkt_total_q = '0;
        buf_full_drop_pkt_total_q[63: 0] = buf_full_drop_pkt_total_buf_full_drop_pkt_total_q;
    end

    // masked version of return data
    assign buf_full_drop_pkt_total_rdata = buf_full_drop_pkt_total_sw_rd ? buf_full_drop_pkt_total_q : 'b0;

    // ------------------------------------------------------------
    // Field: buf_full_drop_pkt_total (wire)
    // ------------------------------------------------------------
    assign buf_full_drop_pkt_total_buf_full_drop_pkt_total_anded = & buf_full_drop_pkt_total_buf_full_drop_pkt_total_q;
    assign buf_full_drop_pkt_total_buf_full_drop_pkt_total_ored  = | buf_full_drop_pkt_total_buf_full_drop_pkt_total_q;
    assign buf_full_drop_pkt_total_buf_full_drop_pkt_total_xored = ^ buf_full_drop_pkt_total_buf_full_drop_pkt_total_q;

    // next hardware value
    assign buf_full_drop_pkt_total_buf_full_drop_pkt_total_next = buf_full_drop_pkt_total_buf_full_drop_pkt_total_wdata;
    assign buf_full_drop_pkt_total_buf_full_drop_pkt_total_q = buf_full_drop_pkt_total_buf_full_drop_pkt_total_next;
            
        
    // ============================================================
    // Register: NOT_READY_DROP_PKT_TOTAL
    //    [63: 0] not_ready_drop_pkt_total: hw=w     sw=r     reset=0x0
    // ============================================================
    logic                  not_ready_drop_pkt_total_decode;
    logic                  not_ready_drop_pkt_total_sw_wr;
    logic                  not_ready_drop_pkt_total_sw_rd;
    logic [DATA_WIDTH-1:0] not_ready_drop_pkt_total_q;

    assign not_ready_drop_pkt_total_decode = (addr == (ADDR_OFFSET+'h0+'h38));
    assign not_ready_drop_pkt_total_sw_wr = sw_wr && not_ready_drop_pkt_total_decode;
    assign not_ready_drop_pkt_total_sw_rd = sw_rd && not_ready_drop_pkt_total_decode;

    always_comb begin
        not_ready_drop_pkt_total_q = '0;
        not_ready_drop_pkt_total_q[63: 0] = not_ready_drop_pkt_total_not_ready_drop_pkt_total_q;
    end

    // masked version of return data
    assign not_ready_drop_pkt_total_rdata = not_ready_drop_pkt_total_sw_rd ? not_ready_drop_pkt_total_q : 'b0;

    // ------------------------------------------------------------
    // Field: not_ready_drop_pkt_total (wire)
    // ------------------------------------------------------------
    assign not_ready_drop_pkt_total_not_ready_drop_pkt_total_anded = & not_ready_drop_pkt_total_not_ready_drop_pkt_total_q;
    assign not_ready_drop_pkt_total_not_ready_drop_pkt_total_ored  = | not_ready_drop_pkt_total_not_ready_drop_pkt_total_q;
    assign not_ready_drop_pkt_total_not_ready_drop_pkt_total_xored = ^ not_ready_drop_pkt_total_not_ready_drop_pkt_total_q;

    // next hardware value
    assign not_ready_drop_pkt_total_not_ready_drop_pkt_total_next = not_ready_drop_pkt_total_not_ready_drop_pkt_total_wdata;
    assign not_ready_drop_pkt_total_not_ready_drop_pkt_total_q = not_ready_drop_pkt_total_not_ready_drop_pkt_total_next;
            
        
    // ============================================================
    // Register: NETRX_IF_PKT_CNT
    //    [63: 0]     netrx_if_pkt_cnt: hw=w     sw=r     reset=0x0
    // ============================================================
    logic                  netrx_if_pkt_cnt_decode;
    logic                  netrx_if_pkt_cnt_sw_wr;
    logic                  netrx_if_pkt_cnt_sw_rd;
    logic [DATA_WIDTH-1:0] netrx_if_pkt_cnt_q;

    assign netrx_if_pkt_cnt_decode = (addr == (ADDR_OFFSET+'h0+'h40));
    assign netrx_if_pkt_cnt_sw_wr = sw_wr && netrx_if_pkt_cnt_decode;
    assign netrx_if_pkt_cnt_sw_rd = sw_rd && netrx_if_pkt_cnt_decode;

    always_comb begin
        netrx_if_pkt_cnt_q = '0;
        netrx_if_pkt_cnt_q[63: 0] = netrx_if_pkt_cnt_netrx_if_pkt_cnt_q;
    end

    // masked version of return data
    assign netrx_if_pkt_cnt_rdata = netrx_if_pkt_cnt_sw_rd ? netrx_if_pkt_cnt_q : 'b0;

    // ------------------------------------------------------------
    // Field: netrx_if_pkt_cnt (wire)
    // ------------------------------------------------------------
    assign netrx_if_pkt_cnt_netrx_if_pkt_cnt_anded = & netrx_if_pkt_cnt_netrx_if_pkt_cnt_q;
    assign netrx_if_pkt_cnt_netrx_if_pkt_cnt_ored  = | netrx_if_pkt_cnt_netrx_if_pkt_cnt_q;
    assign netrx_if_pkt_cnt_netrx_if_pkt_cnt_xored = ^ netrx_if_pkt_cnt_netrx_if_pkt_cnt_q;

    // next hardware value
    assign netrx_if_pkt_cnt_netrx_if_pkt_cnt_next = netrx_if_pkt_cnt_netrx_if_pkt_cnt_wdata;
    assign netrx_if_pkt_cnt_netrx_if_pkt_cnt_q = netrx_if_pkt_cnt_netrx_if_pkt_cnt_next;
            
        
    // ============================================================
    // Register: VIRTIO_RX_BUF_DFX_ERR
    //    [63: 0] virtio_rx_buf_dfx_err: hw=rw    sw=rw    reset=0x0
    // ============================================================
    logic                  virtio_rx_buf_dfx_err_decode;
    logic                  virtio_rx_buf_dfx_err_sw_wr;
    logic                  virtio_rx_buf_dfx_err_sw_rd;
    logic [DATA_WIDTH-1:0] virtio_rx_buf_dfx_err_q;

    assign virtio_rx_buf_dfx_err_decode = (addr == (ADDR_OFFSET+'h0+'h100));
    assign virtio_rx_buf_dfx_err_sw_wr = sw_wr && virtio_rx_buf_dfx_err_decode;
    assign virtio_rx_buf_dfx_err_sw_rd = sw_rd && virtio_rx_buf_dfx_err_decode;

    always_comb begin
        virtio_rx_buf_dfx_err_q = '0;
        virtio_rx_buf_dfx_err_q[63: 0] = virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q;
    end

    // masked version of return data
    assign virtio_rx_buf_dfx_err_rdata = virtio_rx_buf_dfx_err_sw_rd ? virtio_rx_buf_dfx_err_q : 'b0;

    // ------------------------------------------------------------
    // Field: virtio_rx_buf_dfx_err
    // ------------------------------------------------------------
    assign virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_anded = & virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q;
    assign virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_ored  = | virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q;
    assign virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_xored = ^ virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q;

    // next hardware value
    assign virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_next = virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_wdata;

    //! main storage
    always_ff @ (posedge clk)
    if (rst) begin
        virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q <= 64'd0;
    end else begin
        // Hardware Write
        if (virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_we) begin
            virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q <= virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_next;
        end
        // Software write
        else if (virtio_rx_buf_dfx_err_sw_wr) begin
            virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q <= ~sw_masked_data[63: 0] &  virtio_rx_buf_dfx_err_virtio_rx_buf_dfx_err_q;
        end
    end
            
        
    // ============================================================
    // Register: VIRTIO_RX_BUF_DFX_LINK_ERR
    //    [63: 0] virtio_rx_buf_dfx_link_err: hw=rw    sw=rw    reset=0x0
    // ============================================================
    logic                  virtio_rx_buf_dfx_link_err_decode;
    logic                  virtio_rx_buf_dfx_link_err_sw_wr;
    logic                  virtio_rx_buf_dfx_link_err_sw_rd;
    logic [DATA_WIDTH-1:0] virtio_rx_buf_dfx_link_err_q;

    assign virtio_rx_buf_dfx_link_err_decode = (addr == (ADDR_OFFSET+'h0+'h108));
    assign virtio_rx_buf_dfx_link_err_sw_wr = sw_wr && virtio_rx_buf_dfx_link_err_decode;
    assign virtio_rx_buf_dfx_link_err_sw_rd = sw_rd && virtio_rx_buf_dfx_link_err_decode;

    always_comb begin
        virtio_rx_buf_dfx_link_err_q = '0;
        virtio_rx_buf_dfx_link_err_q[63: 0] = virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q;
    end

    // masked version of return data
    assign virtio_rx_buf_dfx_link_err_rdata = virtio_rx_buf_dfx_link_err_sw_rd ? virtio_rx_buf_dfx_link_err_q : 'b0;

    // ------------------------------------------------------------
    // Field: virtio_rx_buf_dfx_link_err
    // ------------------------------------------------------------
    assign virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_anded = & virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q;
    assign virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_ored  = | virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q;
    assign virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_xored = ^ virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q;

    // next hardware value
    assign virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_next = virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_wdata;

    //! main storage
    always_ff @ (posedge clk)
    if (rst) begin
        virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q <= 64'd0;
    end else begin
        // Hardware Write
        if (virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_we) begin
            virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q <= virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_next;
        end
        // Software write
        else if (virtio_rx_buf_dfx_link_err_sw_wr) begin
            virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q <= ~sw_masked_data[63: 0] &  virtio_rx_buf_dfx_link_err_virtio_rx_buf_dfx_link_err_q;
        end
    end
            
        
    // ============================================================
    // Register: VIRTIO_RX_BUF_DFX_LINK_STAT
    //    [63: 0] virtio_rx_buf_dfx_link_stat: hw=w     sw=r     reset=0x0
    // ============================================================
    logic                  virtio_rx_buf_dfx_link_stat_decode;
    logic                  virtio_rx_buf_dfx_link_stat_sw_wr;
    logic                  virtio_rx_buf_dfx_link_stat_sw_rd;
    logic [DATA_WIDTH-1:0] virtio_rx_buf_dfx_link_stat_q;

    assign virtio_rx_buf_dfx_link_stat_decode = (addr == (ADDR_OFFSET+'h0+'h200));
    assign virtio_rx_buf_dfx_link_stat_sw_wr = sw_wr && virtio_rx_buf_dfx_link_stat_decode;
    assign virtio_rx_buf_dfx_link_stat_sw_rd = sw_rd && virtio_rx_buf_dfx_link_stat_decode;

    always_comb begin
        virtio_rx_buf_dfx_link_stat_q = '0;
        virtio_rx_buf_dfx_link_stat_q[63: 0] = virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_q;
    end

    // masked version of return data
    assign virtio_rx_buf_dfx_link_stat_rdata = virtio_rx_buf_dfx_link_stat_sw_rd ? virtio_rx_buf_dfx_link_stat_q : 'b0;

    // ------------------------------------------------------------
    // Field: virtio_rx_buf_dfx_link_stat (wire)
    // ------------------------------------------------------------
    assign virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_anded = & virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_q;
    assign virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_ored  = | virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_q;
    assign virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_xored = ^ virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_q;

    // next hardware value
    assign virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_next = virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_wdata;
    assign virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_q = virtio_rx_buf_dfx_link_stat_virtio_rx_buf_dfx_link_stat_next;
            
        
    // ============================================================
    // Register: VIRTIO_RX_BUF_DFX_DROP_STAT
    //    [63: 0] virtio_rx_buf_dfx_drop_stat: hw=w     sw=r     reset=0x0
    // ============================================================
    logic                  virtio_rx_buf_dfx_drop_stat_decode;
    logic                  virtio_rx_buf_dfx_drop_stat_sw_wr;
    logic                  virtio_rx_buf_dfx_drop_stat_sw_rd;
    logic [DATA_WIDTH-1:0] virtio_rx_buf_dfx_drop_stat_q;

    assign virtio_rx_buf_dfx_drop_stat_decode = (addr == (ADDR_OFFSET+'h0+'h208));
    assign virtio_rx_buf_dfx_drop_stat_sw_wr = sw_wr && virtio_rx_buf_dfx_drop_stat_decode;
    assign virtio_rx_buf_dfx_drop_stat_sw_rd = sw_rd && virtio_rx_buf_dfx_drop_stat_decode;

    always_comb begin
        virtio_rx_buf_dfx_drop_stat_q = '0;
        virtio_rx_buf_dfx_drop_stat_q[63: 0] = virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_q;
    end

    // masked version of return data
    assign virtio_rx_buf_dfx_drop_stat_rdata = virtio_rx_buf_dfx_drop_stat_sw_rd ? virtio_rx_buf_dfx_drop_stat_q : 'b0;

    // ------------------------------------------------------------
    // Field: virtio_rx_buf_dfx_drop_stat (wire)
    // ------------------------------------------------------------
    assign virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_anded = & virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_q;
    assign virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_ored  = | virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_q;
    assign virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_xored = ^ virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_q;

    // next hardware value
    assign virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_next = virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_wdata;
    assign virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_q = virtio_rx_buf_dfx_drop_stat_virtio_rx_buf_dfx_drop_stat_next;

endmodule: virtio_rx_buf_dfx
